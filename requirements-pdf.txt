# PDF import - pdf_to_csv.py (required)
pdfplumber>=0.10.0

# Optional: for AI-powered PDF parsing in pdf_to_csv.py (set ANTHROPIC_API_KEY)
# Install separately if needed: pip install anthropic
# anthropic>=0.39.0
